# Jetson edge-LLM testbed — Python tooling dependencies.
#
# The sweep/orchestration tooling under experiments/ is intentionally
# STDLIB-ONLY (subprocess, csv, re, dataclasses, argparse, pathlib,
# statistics) so it runs on a bare `python3` with no install step and the
# thesis results stay reproducible without pinning a dependency tree.
#
# Add analysis-only deps (e.g. pandas, matplotlib) below if/when needed for
# post-processing — keep the data-collection path dependency-free.

# Dataset download (Phase A — RefDrone + VisDrone via HuggingFace Hub)
huggingface_hub[cli]>=1.19.0
