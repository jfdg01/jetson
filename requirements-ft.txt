# Direct (human-level) deps for the single venv (.venv-ft).
# This is the file you EDIT; the fully-pinned transitive set lives in
# requirements-ft.lock.txt (regenerate with `uv pip freeze` after any change).
# torch/torchvision are +cu124 wheels off PyTorch's CUDA index, not PyPI:
--extra-index-url https://download.pytorch.org/whl/cu124

accelerate==1.14.0
bitsandbytes==0.49.2
datasets==5.0.0
gguf==0.19.0
huggingface_hub==0.36.2
# contrib build (superset of opencv-python) for TrackerCSRT_create — the Level-2
# tracker. ultralytics declares opencv-python>=4.6.0 transitively; this overrides it.
opencv-contrib-python==4.13.0.92
peft==0.19.1
pillow==12.2.0
protobuf==4.25.9
scipy==1.17.1
sentencepiece==0.2.1
torch==2.6.0+cu124
torchvision==0.21.0+cu124
pymavlink==2.4.49
tqdm==4.68.2
transformers==4.57.6
num2words  # SmolVLM (arm E) processor hard-dep
einops  # Florence-2 (arm D) remote modeling code
timm  # Florence-2 (arm D) DaViT vision tower
sam2  # SAM2.1 memory-carry tracker (temporal-acquire-carry Phase 0)
carla==0.9.16  # CARLA client API (P6.1 renderer swap); server is a separate 8.3 GB package, not pip
matplotlib  # proof figures (make_proof.py); P6.1 onward
